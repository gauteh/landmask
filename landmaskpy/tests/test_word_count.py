# -*- coding: utf-8 -*-
import os

import pytest
import landmask

def test_word_count_rust_parallel(benchmark):
    # count = benchmark(word_count.WordCounter(path).search, "is")
    # assert count == 10000
    pass

